/// @desc

#region DEBUGGING

/*
for (var xx = 0; xx < ds_grid_width(global.item_data); xx ++){
	for (var yy = 0; yy < ds_grid_height(global.item_data); yy ++) {
		var statToShow = global.item_data[# xx, yy];
		
		var drawX = (32 + (xx * 48));
		var drawY = (32 + (yy * 16));
		
		draw_set_font(fnt_display_data);
		draw_text(drawX, drawY, statToShow);
	}
}
*/

#endregion




